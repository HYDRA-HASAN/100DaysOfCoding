"use strict";
// Define a function to be executed after the delay
function delayedFunction() {
    console.log("This code will be executed after the delay");
}
// Call setTimeout() to schedule the execution of the delayedFunction after a 2-second delay
setTimeout(delayedFunction, 2000);
